<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>
        <?php if (!empty($name)) {
            echo htmlspecialchars($name);
        } ?>
    </title>
</head>
<body>
    <?php
        if(isset($test))
            echo $test;
    ?>
</body>
</html>